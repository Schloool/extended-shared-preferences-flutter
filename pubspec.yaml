name: extended_shared_preferences
description: An extension for Shared Preferences supporting better constant values, lists and enums.
version: 0.0.1
repository: https://github.com/Schloool/web-response-cache-flutter

environment:
  sdk: '>=2.19.0-444.0.dev <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
